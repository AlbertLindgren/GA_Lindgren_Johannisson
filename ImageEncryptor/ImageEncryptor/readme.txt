Dependencies (Skriv detta i cmd, kommandotolken):
gem install dry-cli

To use imagecrypt:
ruby imagecrypt.rb

ruby imagecrypt.rb encrypt <Message> <Path>

ruby imagecrypt.rb decrypt <Path>

--------------------------------------------------------
To install Rice use: gem install rice

write "ridk enable" in the terminal